extends base 

block maincontents 
  .login__form
    form(method="post").video__form
      input(type="text" name="title" placeholder="Title" value=video.title)
      input(type="text" name="tags" placeholder="Hashtags (seperate comma)" value=video.tags)
      button Edit!
  